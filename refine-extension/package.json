{
  "name": "refine-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node clean.js",
    "build": "npm run clean && node build.js && node copyStatic.js",
    "watch": "node build.js --watch",
    "test": "vitest"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "esbuild": "^0.21.5",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
